# Single production VServer. Connection is over SSH as user `tom` on the
# standard port; the private key and known_hosts are supplied at run time
# (--private-key + ANSIBLE_SSH_COMMON_ARGS), never committed here.
[web]
lausi95.net

[web:vars]
ansible_user=tom
ansible_port=22
ansible_python_interpreter=/usr/bin/python3
