# Managed by Ansible (ansible/site.yml). Do not edit on the server.
# The one per-deploy variable: the immutable image tag (git SHA) this host runs.
# docker compose auto-reads this file for ${IMAGE_TAG} in docker-compose.yml.
IMAGE_TAG={{ image_tag }}
