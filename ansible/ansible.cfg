[defaults]
inventory = inventory.ini
host_key_checking = True
# Readable task output without depending on a separate callback collection.
callback_result_format = yaml
# Deploy over a persistent SSH connection; keeps the compose pull/up snappy.
[ssh_connection]
pipelining = True
