# Vite as the build tool

StringPro is a pure client-side SPA with no server-rendering requirements. Create React App is officially deprecated and unmaintained. Next.js would add SSR complexity we don't need. Vite with the React + TypeScript template gives fast HMR, a minimal config surface, and is the community standard for new React SPAs as of 2025.
